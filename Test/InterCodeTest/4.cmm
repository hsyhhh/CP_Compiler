/* 求阶乘 */
int fact(int n) {
	if(n == 1)
		return n;
	else
		return (n * fact(n - 1));
}

int main() {
	int m, result = -1;
	write(result); /* 测试变量声明时的赋值 */
	m = read();
	if(m > 1)
		result = fact(m);
	else
		result = 1;
	write(result);
	return 0;
}